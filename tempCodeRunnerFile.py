 # img = Image.open(r"images\fac.jpg")
        # img = img.resize((1100,100), Image.ANTIALIAS)
        # self.photoimg = ImageTk.PhotoImage(img)

        # f_lbl = Label(self.root, image=self.photoimg)
        # f_lbl.place(x=0, y=-10, width=1300, height=150)